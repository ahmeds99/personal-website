---
import { fromDateToFormattedString } from "src/scripts/utils";

const { post } = Astro.props;
const { title, mediaURL, description, newsSite, author, pubDate } = post;

const date = fromDateToFormattedString(pubDate);
---

<li class="mt-16">
  <h2 class="font-bold text-center mb-4">
    <a href={mediaURL} class="hover-underline">🔗 {title}</a>
  </h2>
  <p>📅: {date}</p>
  <p>
    📰: <a href={"https://" + newsSite} class="hover-underline">{newsSite}</a>
  </p>
  <p class="font-medium">{description}</p>
  <p class="italic mt-2">Skrevet av: {author}</p>
</li>

<style>
  .hover-underline:hover {
    text-decoration: underline;
    text-decoration-color: #9c9cef;
    text-decoration-thickness: 3px;
    text-underline-offset: 5px;
  }
</style>
